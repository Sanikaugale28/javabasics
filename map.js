const number=[1,2,3,4,5];
const squaredNumbers= number.map(number=>number*number);
console.log(squaredNumbers);

let carname=["breeza","nexon"];
let bikename= carname.map(carname=> carname + carname)
console.log(bikename);

let age=[21.6,21,23.50,50,39.0];
let years= age.map(age=> age-age)
console.log(years);

